待实现功能：
回测与仿真：`src/arb/backtest/loader.py`（历史 funding/price 数据加载）、`src/arb/backtest/simulator.py`（持仓与成交仿真）、`src/arb/backtest/report.py`（收益、回撤、容量分析）；编写回放测试与结果校验测试。

已实现功能：
已新增 `src/arb/backtest/loader.py`、`src/arb/backtest/simulator.py`、`src/arb/backtest/report.py` 以及 `src/arb/backtest/__init__.py`（完成历史 funding/price 数据加载、funding 回放仿真、收益/回撤/平均流动性报告）；已新增 `tests/test_backtest/test_backtest.py`（覆盖历史加载、策略回放与结果一致性测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `83 passed`）。
