待实现功能：
阈值驱动资金费率回测状态机：更新 `src/arb/backtest/simulator.py`，支持 `open_threshold`、`close_threshold`、`hysteresis`，实现“只有 funding 高于开仓阈值才开仓、低于平仓阈值才平仓”的持仓状态机；更新 `src/arb/backtest/__init__.py`；补充测试 `tests/test_backtest/test_backtest.py`，验证无仓/开仓/持仓/平仓路径。

已实现功能：
已更新 `src/arb/backtest/simulator.py`，支持 `open_threshold`、`close_threshold`、`hysteresis`，并实现“等待 -> 开仓 -> 持仓 -> 平仓”的阈值驱动状态机；已保持无阈值时的全程持有回放逻辑；已补充测试 `tests/test_backtest/test_backtest.py`，验证开平仓阈值与滞回逻辑。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_backtest/test_backtest.py`，结果 `6 passed`）。
