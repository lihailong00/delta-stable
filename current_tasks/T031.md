待实现功能：
多交易所真实扫描运行时：`src/arb/runtime/exchange_manager.py`（统一管理各交易所 live client）、`src/arb/runtime/realtime_scanner.py`（实时采集 -> 标准化 -> 扫描循环）、`src/arb/runtime/pipeline.py`（机会输出、持久化、监控联动）；支持 Binance/OKX/Bybit/Gate/Bitget/HTX 并行公共行情扫描；编写运行时集成测试与 dry-run 测试。

已实现功能：
已新增 `src/arb/runtime/exchange_manager.py`（统一管理多交易所 live runtime，支持并行 `public_ping`、并行快照采集、健康心跳与失败告警）；已新增 `src/arb/runtime/realtime_scanner.py`（实时采集 -> FundingScanner -> 周期扫描循环，支持 dry-run）；已新增 `src/arb/runtime/pipeline.py`（机会格式化输出、ticker/funding 持久化、指标统计）；已新增 `tests/test_runtime/test_realtime_scanner.py`（覆盖多交易所并行扫描、运行时失败恢复与 dry-run 输出测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `148 passed`）。
