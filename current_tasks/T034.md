待实现功能：
基于组合重构 `src/arb/runtime/binance_runtime.py`、`src/arb/runtime/okx_runtime.py`、`src/arb/runtime/bybit_runtime.py`、`src/arb/runtime/gate_runtime.py`、`src/arb/runtime/bitget_runtime.py`、`src/arb/runtime/htx_runtime.py`：移除重复的 `collector/on_message/WebSocketSession` 模板代码，改为装配共享 helper；保持各交易所差异化能力（`login/auth`、`ticker/funding/orderbook` 频道）不变；更新对应 `tests/test_runtime/test_*_runtime.py`。

已实现功能：
已重构 `src/arb/runtime/binance_runtime.py`、`src/arb/runtime/okx_runtime.py`、`src/arb/runtime/bybit_runtime.py`、`src/arb/runtime/gate_runtime.py`、`src/arb/runtime/bitget_runtime.py`、`src/arb/runtime/htx_runtime.py`：移除重复的 `MarketDataCollector/WebSocketSession/on_message` 模板逻辑，统一改为装配 `SnapshotService`、`PublicStreamService`、`PrivateSessionService`；保留各交易所差异化能力（`login/auth`、`orderbook/ticker/funding`）不变；现有 `tests/test_runtime/test_*_runtime.py` 已作为组合式重构后的回归测试。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `uv run pytest -q`，结果 `157 passed`）。
