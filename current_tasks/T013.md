待实现功能：
风控系统：`src/arb/risk/checks.py`（爆仓距离、基差异常、funding 反转、单腿裸露检查）、`src/arb/risk/limits.py`（杠杆与仓位上限）、`src/arb/risk/killswitch.py`（全局停机与只减仓模式）；编写 mock 测试与故障场景测试。

已实现功能：
已新增 `src/arb/risk/checks.py`、`src/arb/risk/limits.py`、`src/arb/risk/killswitch.py` 以及 `src/arb/risk/__init__.py`（完成爆仓距离、基差异常、funding 反转、单腿裸露检查，杠杆/仓位上限校验，以及全局停机与只减仓模式）；已新增 `tests/test_risk/test_risk.py`（覆盖风险阈值触发、限额校验、只减仓与 kill switch 测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `74 passed`）。
