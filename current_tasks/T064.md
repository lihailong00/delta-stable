待实现功能：
控制面、API 与飞书模型化：重构 `src/arb/control/`、`src/arb/integrations/feishu/`，为 API 请求/响应、service bridge 输出、手工命令 payload、飞书回调、飞书卡片数据、客户端请求体建立明确的 Pydantic 模型；替换 `dict[str, Any]` / `Any` 返回值；更新控制面与飞书相关测试。

已实现功能：
已新增 `src/arb/integrations/feishu/schemas.py`（定义 `FeishuCard`、`FeishuTransportRequest`、`FeishuParsedCallback` 等 Pydantic 模型）；已重构 `src/arb/control/schemas.py`、`src/arb/control/commands.py`、`src/arb/control/deps.py`、`src/arb/control/service_bridge.py`、`src/arb/control/api.py`（将控制面请求/响应、bridge 输出、依赖上下文和 Lite API 路由改为显式模型和协议，移除主链 `Any`/松散 `dict`）；已重构 `src/arb/integrations/feishu/cards.py`、`src/arb/integrations/feishu/client.py`、`src/arb/integrations/feishu/events.py`（卡片构造、客户端请求体、回调解析与命令分发全部基于 Pydantic 模型）；已更新 `tests/test_control/test_control_api.py`、`tests/test_control/test_service_bridge.py`、`tests/test_integrations/test_feishu.py` 相关兼容性验证。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_control/test_control_api.py tests/test_control/test_service_bridge.py tests/test_integrations/test_feishu.py`，结果 `13 passed`）。
