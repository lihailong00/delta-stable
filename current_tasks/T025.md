待实现功能：
Bybit 真实接通：更新 `src/arb/exchange/bybit.py`、`src/arb/ws/bybit.py` 接入真实 HTTP/WS transport；新增 `src/arb/runtime/bybit_runtime.py`（公共行情订阅、private auth、REST/WS 联动）；新增 Bybit 公共与私有 smoke 测试/脚本。

已实现功能：
已新增 `src/arb/runtime/bybit_runtime.py` 并更新 `src/arb/runtime/__init__.py`（完成 Bybit REST/WS 与 `HttpTransport`/`WebSocketSession` 的运行时接线，支持公共 time 校验、私有余额校验、公共快照采集、`orderbook/ticker` 订阅以及 private auth 消息生成与发送）；现有 `src/arb/exchange/bybit.py`、`src/arb/ws/bybit.py` 已可直接接收真实 transport；已新增 `tests/test_runtime/test_bybit_runtime.py`（覆盖公共连通、私有账户校验、private auth 与 `orderbook/ticker` 订阅测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `118 passed`）。
