待实现功能：
Pydantic 基础设施与建模约束：更新 `pyproject.toml` 引入 `pydantic`，新增统一基础模型（如 `src/arb/schemas/base.py`），约束 `extra="forbid"`、统一序列化/时间/Decimal 配置；补充项目级类型约束说明文档，明确哪些边界必须用 `BaseModel`、哪些位置禁止 `Any` 和松散 `dict`；补充基础测试验证模型校验、序列化和默认配置。

已实现功能：
已更新 `pyproject.toml` 引入 `pydantic` 与类型检查依赖；已新增 `src/arb/schemas/base.py`、`src/arb/schemas/__init__.py`（统一 `ArbModel` / `ArbFrozenModel`、`extra="forbid"`、统一 `to_dict()` 和映射辅助方法）；已新增 `docs/pydantic-modeling-guide.md` 约束主链建模边界，并补充基础测试 `tests/test_schemas/test_base.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行基础 schema 测试，确认 `pydantic` 依赖、模型默认配置和序列化行为生效）。
