待实现功能：
仓位对账与重启恢复：新增 `src/arb/portfolio/reconciler.py`（交易所真实仓位/订单与本地状态对账）、更新 `src/arb/storage/repository.py`、`src/arb/storage/schema.sql`（保存工作流状态、订单状态历史、成交回报）、新增 `src/arb/runtime/recovery.py`（进程重启后恢复未完成工作流）；补充测试 `tests/test_portfolio/test_reconciler.py`、`tests/test_storage/test_repository.py`、`tests/test_runtime/test_recovery.py`。

已实现功能：
已新增 `src/arb/portfolio/reconciler.py`，实现本地仓位/订单与交易所真实状态对账；已更新 `src/arb/storage/repository.py` 与 `src/arb/storage/schema.sql`，新增工作流状态、订单状态历史、扩展成交与仓位字段；已新增 `src/arb/runtime/recovery.py`，支持进程重启后恢复未完成工作流并输出对账计划；已补充测试 `tests/test_portfolio/test_reconciler.py`、`tests/test_storage/test_repository.py`、`tests/test_runtime/test_recovery.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_portfolio/test_reconciler.py`、`UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_storage/test_repository.py`、`UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_runtime/test_recovery.py`，结果分别为 `1 passed`、`5 passed`、`1 passed`）。
