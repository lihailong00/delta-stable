待实现功能：
回测数据链 Pydantic 化；为 `src/arb/backtest/dataset_fetcher.py`、`src/arb/backtest/loader.py`、`src/arb/backtest/report.py` 新增明确 schema，覆盖 funding CSV 行、kline CSV 行、合并后的回测记录、历史点输入和回测报告输出；去掉这些模块中的 `dict[str, str]`、`dict[str, Any]` 作为主链数据结构；补充测试。

已实现功能：
已新增 `src/arb/backtest/schemas.py`（补充 `FundingCsvRow`、`KlineCsvRow`、`MergedBacktestRow`、`HistoricalPoint`、`BacktestResult`、`BacktestReport` 等回测主链 schema）；已更新 `src/arb/backtest/dataset_fetcher.py`、`src/arb/backtest/loader.py`、`src/arb/backtest/report.py`、`src/arb/backtest/simulator.py`（主链改为显式 Pydantic 模型，补回测数据集序列化/反序列化契约，去掉核心路径上的松散字典）；已补充 `tests/test_backtest/test_dataset_fetcher.py`、`tests/test_backtest/test_backtest.py` 对 schema 和兼容路径的断言。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_backtest/test_backtest.py tests/test_backtest/test_dataset_fetcher.py`，结果 `24 passed`）。
