待实现功能：
仓位与资金管理：`src/arb/portfolio/positions.py`（统一持仓视图）、`src/arb/portfolio/balances.py`（账户余额与可用保证金）、`src/arb/portfolio/allocator.py`（单币、单所、组合级资金分配）；编写 mock 测试。

已实现功能：
已新增 `src/arb/portfolio/positions.py`、`src/arb/portfolio/balances.py`、`src/arb/portfolio/allocator.py` 以及 `src/arb/portfolio/__init__.py`（完成统一持仓视图、净敞口与对冲比率计算、账户余额/可用保证金管理、单币/单所/组合级资金分配）；已新增 `tests/test_portfolio/test_portfolio.py`（覆盖净敞口、保证金占用与资金分配限制测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `71 passed`）。
