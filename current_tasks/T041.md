待实现功能：
平仓与应急减仓工作流：新增 `src/arb/workflows/close_position.py`（正常平仓、funding 反转平仓、持仓超时平仓、只减仓模式）、更新 `src/arb/risk/killswitch.py`、`src/arb/risk/checks.py`（与平仓工作流联动）；补充测试 `tests/test_workflows/test_close_position.py`、`tests/test_risk/test_risk.py` 覆盖平仓优先级、强制减仓、失败重试。

已实现功能：
已新增 `src/arb/workflows/close_position.py`，实现正常平仓、funding 反转平仓、持仓超时平仓与 reduce-only 应急减仓，并支持失败后按剩余数量重试；已更新 `src/arb/risk/killswitch.py` 与 `src/arb/risk/checks.py`，补充平仓原因优先级、持仓超时检查、kill switch 的 reduce-only/关闭原因联动；已补充测试 `tests/test_workflows/test_close_position.py`、`tests/test_risk/test_risk.py`，覆盖平仓优先级、强制减仓和失败重试。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_workflows/test_close_position.py` 与 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_risk/test_risk.py`，结果分别为 `3 passed`、`4 passed`）。
