待实现功能：
套利服务与工作流状态建模：重构 `src/arb/runtime/funding_arb_service.py`、`src/arb/runtime/cross_exchange_funding_service.py`、`src/arb/runtime/bootstrap.py`、`src/arb/runtime/private_event_hub.py`、`src/arb/runtime/persisted_execution.py`，将 active position、workflow state、execution persistence、private event payload 等改为 Pydantic 模型；清理服务层的 `snapshot: dict[str, Any]`、`execution: Any` 等宽泛类型；补充服务与集成测试。

已实现功能：
已新增 `src/arb/runtime/schemas.py`，为 active funding/cross-exchange 仓位状态、跨所机会、workflow state record、recovery plan 建模；已更新 `src/arb/runtime/funding_arb_service.py`、`src/arb/runtime/cross_exchange_funding_service.py`、`src/arb/runtime/recovery.py`、`src/arb/execution/private_event_hub.py`、`src/arb/runtime/private_streams.py`、`src/arb/runtime/__init__.py`，将主服务状态、恢复计划和私有事件 payload 改为显式模型，并移除服务层的 `snapshot: dict[str, Any]`、`execution: Any` 等宽泛类型。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_runtime/test_funding_arb_service.py tests/test_runtime/test_cross_exchange_funding_service.py tests/test_runtime/test_recovery.py tests/test_execution/test_private_event_hub.py`，结果 `6 passed`）。
