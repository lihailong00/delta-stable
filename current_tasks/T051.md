待实现功能：
真实环境配置与 testnet/live 装配：新增 `src/arb/bootstrap/live_runtime_factory.py`（按环境变量和配置组装 Binance/OKX/Bybit/Gate/Bitget/HTX runtime，支持 testnet/live 切换）、更新 `src/arb/config/live.py`、`src/arb/settings/exchanges.py`、`docs/live-exchange-onboarding.md`，补充测试 `tests/test_bootstrap/test_live_runtime_factory.py`、`tests/test_live_runtime.py`。

已实现功能：
已新增 `src/arb/bootstrap/live_runtime_factory.py`（集中按环境变量和 `LiveRuntimeConfig` 组装 Binance/OKX/Bybit/Gate/Bitget/HTX runtime，并按 `testnet/live` 解析 REST/WS 地址）；已更新 `src/arb/config/live.py`、`src/arb/settings/exchanges.py`、`docs/live-exchange-onboarding.md`（新增 `use_testnet`、endpoint 解析辅助方法和装配说明）；已补充测试 `tests/test_bootstrap/test_live_runtime_factory.py`、`tests/test_live_runtime.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_bootstrap/test_live_runtime_factory.py tests/test_live_runtime.py`，结果 `7 passed`）。
