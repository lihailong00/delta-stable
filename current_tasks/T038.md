待实现功能：
订单状态跟踪器：新增 `src/arb/execution/order_tracker.py`（轮询订单状态、等待成交、部分成交检测、超时撤单、补单决策）、更新 `src/arb/execution/executor.py`（接入 tracker 而不是只看单次下单返回）、更新 `src/arb/execution/__init__.py`；补充测试 `tests/test_execution/test_order_tracker.py`、`tests/test_execution/test_execution_engine.py` 覆盖超时、部分成交、重复查询、撤单回收等场景。

已实现功能：
已新增 `src/arb/execution/order_tracker.py`（完成订单轮询、终态判断、超时撤单、成交回报读取与同步/异步 sleep 兼容）；已更新 `src/arb/execution/executor.py`、`src/arb/execution/__init__.py`（执行器接入 tracker，先提交后追踪，再根据最终成交量做补单）；已新增 `tests/test_execution/test_order_tracker.py` 并更新 `tests/test_execution/test_execution_engine.py`（覆盖终态成交、超时撤单、tracker 驱动的部分成交补单）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_execution/test_order_tracker.py tests/test_execution/test_execution_engine.py`，结果 `8 passed`）。
