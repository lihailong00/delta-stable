待实现功能：
`src/arb/exchange/okx.py`（REST：签名、账户/余额、现货与永续行情、funding、下单/撤单、instId 符号转换）、`src/arb/ws/okx.py`（WS：books/tickers/funding-rate 订阅、登录鉴权、心跳与重连、标准化消息）、编写 mock 测试 `tests/test_exchange/test_okx_exchange.py`、`tests/test_ws/test_ws_okx.py`。

已实现功能：
已新增 `src/arb/exchange/okx.py`（实现 OKX REST：`OK-ACCESS-*` 请求头、`timestamp + method + requestPath + body` 预签名、Base64 HMAC-SHA256、余额/行情/funding/下单撤单接口、`BTC-USDT(-SWAP) ↔ BTC/USDT` 符号转换、WS 登录签名参数）；已新增 `src/arb/ws/okx.py`（`books`、`tickers`、`funding-rate` 订阅、login 消息构造、ping/pong 与标准化消息解析）；已更新导出并新增 mock 测试 `tests/test_exchange/test_okx_exchange.py`、`tests/test_ws/test_ws_okx.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `32 passed`）。
