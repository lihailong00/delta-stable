待实现功能：
`src/arb/exchange/gate.py`（REST：HMAC-SHA512 签名、载荷 `method\npath\nquery\nhash(body)\nts`、`KEY/SIGN/Timestamp` Header、`BTC_USDT ↔ BTC/USDT` 符号转换）、`src/arb/ws/gate.py`（WS：`wss://api.gateio.ws/ws/v4/`、JSON subscribe、`spot.order_book`/合约行情频道、30s ping/pong、标准化消息）、编写 mock 测试 `tests/test_exchange/test_gate_exchange.py`、`tests/test_ws/test_ws_gate.py`。

已实现功能：
已新增 `src/arb/exchange/gate.py`（实现 Gate.io REST：HMAC-SHA512 签名、载荷 `method\npath\nquery\nhash(body)\nts`、`KEY/SIGN/Timestamp` Header、`BTC_USDT ↔ BTC/USDT` 符号转换、现货/合约行情与私有请求封装）；已新增 `src/arb/ws/gate.py`（`wss://api.gateio.ws/ws/v4/`、JSON subscribe、`spot.order_book` 频道、30s ping/pong 心跳逻辑、消息解析并发出 `orderbook.update`）；已更新导出并新增 mock 测试 `tests/test_exchange/test_gate_exchange.py`、`tests/test_ws/test_ws_gate.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `47 passed`）。
