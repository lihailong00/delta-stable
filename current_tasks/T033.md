待实现功能：
`src/arb/runtime/protocols.py`（定义 live runtime 协议：`public_ping`、`validate_private_access`、`fetch_public_snapshot` 等统一接口）；`src/arb/runtime/streaming.py`（抽离公共 WS 订阅/消息收集 helper、private login/auth session helper）；`src/arb/runtime/snapshots.py`（抽离 `collector.collect_snapshot` 组合服务）；补充面向组合 helper 的单元测试 `tests/test_runtime/test_runtime_components.py`。

已实现功能：
已新增 `src/arb/runtime/protocols.py`（定义 `LiveRuntimeProtocol` 与 WS 组合接口协议）；已新增 `src/arb/runtime/snapshots.py`（抽离 `MarketDataCollector.collect_snapshot/ingest_ws_message` 的组合服务 `SnapshotService`）；已新增 `src/arb/runtime/streaming.py`（抽离公共 WS 订阅 `PublicStreamService` 与 private login/auth 会话 `PrivateSessionService`）；已更新 `src/arb/runtime/__init__.py` 并新增测试 `tests/test_runtime/test_runtime_components.py`（覆盖协议契约、snapshot helper、公有 WS 组合 helper、private session helper）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `uv run pytest -q`，结果 `157 passed`）。
