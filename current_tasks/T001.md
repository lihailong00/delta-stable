待实现功能：
项目骨架与公共模块：`src/arb/__init__.py`、`src/arb/config.py`、`src/arb/logging.py`、`src/arb/models.py`、`src/arb/errors.py`；统一环境变量加载、日志初始化、基础数据结构（ticker/orderbook/funding/order/position）；编写 smoke 测试与导入测试。

已实现功能：
已新增 `.gitignore`（忽略 `.idea/`、`.venv/` 与 Python 缓存文件）；已新增 `src/arb/__init__.py`、`src/arb/config.py`、`src/arb/logging.py`、`src/arb/models.py`、`src/arb/errors.py`（完成环境变量配置加载、日志初始化、基础数据结构与项目异常定义）；已新增 `tests/test_config.py`、`tests/test_imports.py`、`tests/test_models.py`（覆盖配置读取、模块导入 smoke 测试、数据模型序列化测试），并更新 `pyproject.toml`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `6 passed`）。
