待实现功能：
开仓工作流：新增 `src/arb/workflows/open_position.py`（现货多 + 永续空开仓编排、报价校验、双腿限时成交、失败回滚、最大裸露时间控制）、更新 `src/arb/strategy/spot_perp.py` 与 `src/arb/execution/router.py`（接入 maker/taker、紧急切 taker、交易所可用性）；补充测试 `tests/test_workflows/test_open_position.py`、`tests/test_strategy/test_strategy_engine.py`。

已实现功能：
已新增 `src/arb/workflows/open_position.py` 与 `src/arb/workflows/__init__.py`，实现现货多 + 永续空开仓编排、报价校验、maker/taker 路由、超时后紧急切 taker、失败暴露回滚与最大裸露时间控制；已更新 `src/arb/strategy/spot_perp.py` 增加开仓报价检查，更新 `src/arb/execution/router.py` 支持 taker 滑点报价和紧急升级判定；已补充测试 `tests/test_workflows/test_open_position.py`、`tests/test_strategy/test_strategy_engine.py`，覆盖开仓成功、taker 回退和裸露回滚。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_workflows/test_open_position.py` 与 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_strategy/test_strategy_engine.py`，结果分别为 `3 passed`、`6 passed`）。
