待实现功能：
策略阈值按周期归一化：更新 `src/arb/strategy/spot_perp.py`、`src/arb/runtime/funding_arb_service.py`、`src/arb/workflows/open_position.py` 等主链，使开仓/平仓阈值支持基于统一周期（如 hourly 或 daily）判断，而不是直接拿原始 funding 值比较；补充策略与 service 测试。

已实现功能：
已更新 `src/arb/strategy/spot_perp.py`（新增 `threshold_interval_hours`，在开仓/平仓判断前先按统一周期归一化 funding rate）；已更新 `src/arb/workflows/open_position.py`、`src/arb/risk/position_monitor.py`、`src/arb/runtime/funding_arb_service.py`，使 open workflow、持仓监控和主服务都沿用统一周期阈值语义；已更新测试工厂与策略/workflow/risk/service 测试，覆盖按小时阈值比较 `1h` 与 `8h` funding 的行为。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q` 针对 strategy、workflow、risk monitor、funding arb service 的测试，结果 `22 passed`）。
