待实现功能：
回测支持多 funding 周期：更新 `src/arb/backtest/loader.py`、`src/arb/backtest/simulator.py`、`src/arb/backtest/dataset_fetcher.py`，使历史点位可携带 funding 周期信息，回测成本、阈值和 Binance 数据抓取不再写死 `8h`；补充多周期回测与数据抓取测试。

已实现功能：
已更新 `src/arb/backtest/loader.py`（`HistoricalPoint` 增加 `funding_interval_hours` 并支持从 CSV/rows 解析）；已更新 `src/arb/backtest/simulator.py`（阈值回测先将 funding rate 归一化到统一比较周期，再决定开平仓）；已更新 `src/arb/backtest/dataset_fetcher.py`、`scripts/fetch_backtest_dataset.py`（Binance 数据抓取与输出文件不再写死 `8h`，支持 `1h/2h/4h/8h` 周期和 `funding_interval_hours` 列）；已补充 backtest 与 dataset fetcher 测试覆盖多周期历史点位和 CSV 输出。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_backtest/test_backtest.py tests/test_backtest/test_dataset_fetcher.py`，结果 `17 passed`）。
