待实现功能：
资金费率周期建模：为 `src/arb/models.py`、`src/arb/market/schemas.py`、交易所适配与 WS 解析补充 `funding_interval_hours` 或等价周期字段，确保 `1h/2h/4h/8h` 资金费率都能被统一表示；补充相应模型与解析测试。

已实现功能：
已新增 `src/arb/funding/intervals.py`、`src/arb/funding/__init__.py`（统一提取与归一化 `funding_interval_hours`）；已更新 `src/arb/models.py`、`src/arb/market/schemas.py`、`src/arb/ws/schemas.py`，为 `FundingRate` 和 funding 相关 WS payload 增加周期字段；已更新 Binance/OKX/Bybit/Gate/Bitget/HTX 的 REST funding 解析与 Binance/OKX/Bybit/Bitget 的 WS funding 解析；已补充模型、exchange、ws 测试覆盖 `1h/2h/4h/8h` mock 周期字段。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q` 针对模型、exchange、ws 的周期解析测试，结果 `68 passed`）。
