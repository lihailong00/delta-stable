待实现功能：
类型门禁扩大覆盖面；更新 `pyproject.toml` 中的 `mypy` 检查范围，纳入本轮重构后的 backtest/runtime/storage/control/risk/portfolio/config 模块；补充迁移文档，说明哪些地方用 Pydantic、哪些地方用 Protocol、哪些地方保留普通 dict；执行类型检查和回归测试。

已实现功能：
已更新 `pyproject.toml`（`mypy` 文件范围扩大到本轮完成建模/协议收口的 `backtest/runtime/storage/control/risk/portfolio/config/settings/execution/workflows` 主链模块）；已更新 `docs/pydantic-modeling-guide.md`、`docs/type-checking-guide.md`（明确哪些边界用 Pydantic、哪些协作者用 Protocol、哪些地方允许保留普通 dict）；已完成扩展后的类型检查与全量回归测试。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src UV_CACHE_DIR=.uv-cache uv run mypy`，结果 `Success: no issues found in 43 source files`；已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q`，结果 `274 passed`）。
