待实现功能：
实盘配置与安全开关：`src/arb/config/live.py`（live/testnet/dry-run 配置加载）、更新 `src/arb/settings/exchanges.py`（各交易所真实凭证、endpoint、testnet 开关、recv window）、`src/arb/safety/runtime.py`（只读模式、只减仓模式、全局禁下单开关）；编写配置校验测试。

已实现功能：
已将配置层重构为 `src/arb/config/` package，并新增 `src/arb/config/live.py`（完成 live/testnet/dry-run/read-only/orders-enabled 配置加载）；已更新 `src/arb/settings/exchanges.py`（新增真实 endpoint、testnet endpoint、passphrase、recv window 等配置字段与校验）；已新增 `src/arb/safety/runtime.py` 与 `src/arb/safety/__init__.py`（完成只读模式、只减仓模式、全局禁下单开关）；已新增 `tests/test_live_runtime.py`，并扩展 `tests/test_imports.py` 覆盖 live 配置与 safety 模块导入测试。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `107 passed`）。
