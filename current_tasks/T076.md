待实现功能：
控制面命令与响应收口；为 `src/arb/control/api.py`、`src/arb/control/dispatcher.py`、`src/arb/control/service_bridge.py` 统一使用显式 Pydantic 命令结果、列表响应和 workflow 状态模型，移除 `dict[str, Any]` / `dict[str, SerializableValue]` 的业务主链依赖；补充测试。

已实现功能：
已更新 `src/arb/control/deps.py`、`src/arb/control/api.py`、`src/arb/control/dispatcher.py`、`src/arb/control/service_bridge.py`（provider/handler 主链统一返回显式 response schema，旧 dict handler 通过兼容层自动收口到 `CommandResponse`）；已补强 `src/arb/control/schemas.py` 的队列快照模型；已补充 `tests/test_control/test_control_api.py`、`tests/test_control/test_service_bridge.py`、`tests/test_control/test_command_dispatcher.py` 的 typed response 断言。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_control`，结果 `23 passed`）。
