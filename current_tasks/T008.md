待实现功能：
数据存储层：`src/arb/storage/db.py`（数据库连接与迁移入口）、`src/arb/storage/repository.py`（orders/fills/positions/funding/ticks 持久化）、`src/arb/storage/schema.sql` 或迁移脚本；编写 repository mock 测试与基础读写测试。

已实现功能：
已新增 `src/arb/storage/db.py`、`src/arb/storage/repository.py`、`src/arb/storage/schema.sql` 以及 `src/arb/storage/__init__.py`（完成 SQLite 初始化、表结构创建、orders/fills/positions/funding/ticks 持久化与 funding 历史查询）；已新增 `tests/test_storage/test_repository.py`（覆盖建表、订单与仓位写入、funding 历史查询、ticker/fill 落库测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `54 passed`）。
