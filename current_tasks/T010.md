待实现功能：
策略引擎：`src/arb/strategy/spot_perp.py`（现货多 + 永续空）、`src/arb/strategy/perp_spread.py`（跨所永续对冲）、`src/arb/strategy/engine.py`（开仓、持有、平仓、再平衡状态机）；编写策略 mock 测试与回放测试。

已实现功能：
已新增 `src/arb/strategy/spot_perp.py`、`src/arb/strategy/perp_spread.py`、`src/arb/strategy/engine.py` 以及 `src/arb/strategy/__init__.py`（完成同所 `现货多 + 永续空`、跨所永续 spread 策略、开仓/持有/平仓/再平衡状态机与对冲比率计算）；已新增 `tests/test_strategy/test_strategy_engine.py`（覆盖开平仓条件、对冲比例计算、持仓期关闭与再平衡测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `63 passed`）。
