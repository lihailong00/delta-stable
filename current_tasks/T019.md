待实现功能：
飞书集成：`src/arb/integrations/feishu/client.py`（获取 tenant access token、发送消息/卡片、基础重试）、`src/arb/integrations/feishu/events.py`（飞书事件回调、签名校验、challenge 响应）、`src/arb/integrations/feishu/cards.py`（当前仓位、策略状态、手动平仓/暂停操作卡片渲染）；支持飞书查看当前仓位和触发人工操作；编写 mock 测试。

已实现功能：
已新增 `src/arb/integrations/feishu/client.py`、`src/arb/integrations/feishu/events.py`、`src/arb/integrations/feishu/cards.py` 以及集成导出（完成 tenant access token 获取与缓存、消息/卡片发送、基础重试、回调验签、challenge 响应、当前仓位/策略状态/手动操作卡片渲染）；已新增 `tests/test_integrations/test_feishu.py`（覆盖 token 刷新、回调验签、卡片渲染与按钮回调解析测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `95 passed`）。
