待实现功能：
主服务启动入口与装配：新增 `src/arb/bootstrap/funding_arb_app.py`（装配 `LiveExchangeManager`、`RealtimeScanner`、`FundingArbService`、`OpportunityPipeline`、`Repository`、`ControlAPI` 所需依赖）、更新 `src/arb/cli.py`（新增 `funding-arb` / `funding-arb-dry-run` 命令并接入真实 handler）、补充测试 `tests/test_bootstrap/test_funding_arb_app.py`、`tests/test_cli/test_cli.py`。

已实现功能：
已新增 `src/arb/bootstrap/funding_arb_app.py` 与 `src/arb/bootstrap/__init__.py`，实现 `LiveExchangeManager`、`RealtimeScanner`、`FundingArbService`、`OpportunityPipeline`、`Repository`、`ControlAPI` 的薄装配层；已更新 `src/arb/cli.py`，新增 `funding-arb` / `funding-arb-dry-run` 命令；已补充测试 `tests/test_bootstrap/test_funding_arb_app.py`、`tests/test_cli/test_cli.py`，验证启动装配、CLI 参数和 handler 绑定。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_bootstrap/test_funding_arb_app.py tests/test_cli/test_cli.py`，结果 `8 passed`）。
