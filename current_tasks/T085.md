待实现功能：
主仓库回接独立网络库；将 `arb` 主项目中的 `src/arb/net/` 改为兼容层或直接依赖新包，更新 `runtime`、`exchange`、`bootstrap` 对 transport 的导入与装配，确保不破坏现有交易所接入和测试路径；补充迁移测试。

已实现功能：
已将 `src/arb/net/http.py`、`src/arb/net/ws.py`、`src/arb/net/errors.py`、`src/arb/net/schemas.py` 改为对 `typed_transport` 的兼容 re-export 层，主仓库现有 `runtime`、`exchange`、`bootstrap` 仍可保持原导入路径；已把根项目 `pyproject.toml` 的类型检查范围扩展到 `src/typed_transport`，并通过 runtime/交易所回归测试验证抽离后主仓库行为未被破坏。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run --no-sync pytest -q tests/test_net tests/test_runtime/test_runtime_components.py tests/test_runtime/test_binance_runtime.py tests/test_runtime/test_okx_runtime.py tests/test_runtime/test_bybit_runtime.py tests/test_runtime/test_gate_runtime.py tests/test_runtime/test_bitget_runtime.py tests/test_runtime/test_htx_runtime.py`，以及全量 `pytest -q`，均通过）。
