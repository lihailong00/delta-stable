待实现功能：
`src/arb/exchange/binance.py`（REST：签名、账户/余额、现货与永续行情、funding、下单/撤单、符号转换）、`src/arb/ws/binance.py`（WS：bookTicker/depth/funding 订阅、ping/pong、重连与标准化消息）、编写 mock 测试 `tests/test_exchange/test_binance_exchange.py`、`tests/test_ws/test_ws_binance.py`。

已实现功能：
已新增 `src/arb/exchange/binance.py`（实现 Binance REST：HMAC-SHA256 签名、现货与永续行情/深度/funding/余额/下单撤单请求、`BTCUSDT ↔ BTC/USDT` 符号转换、可注入 transport 方便 mock 测试）；已新增 `src/arb/ws/binance.py`（`bookTicker`、`depth`、`markPrice@1s` 订阅、Binance stream 名称构造、订单簿与 funding 标准化消息解析）；已更新导出并新增 mock 测试 `tests/test_exchange/test_binance_exchange.py`、`tests/test_ws/test_ws_binance.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `24 passed`）。
