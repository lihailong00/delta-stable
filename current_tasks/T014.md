待实现功能：
收益归因与报表：`src/arb/pnl/ledger.py`（realized/unrealized/funding/fees/borrow 拆分）、`src/arb/pnl/reports.py`（日维度与策略维度报表）、`src/arb/pnl/export.py`（CSV/JSON 导出）；编写 mock 测试。

已实现功能：
已新增 `src/arb/pnl/ledger.py`、`src/arb/pnl/reports.py`、`src/arb/pnl/export.py` 以及 `src/arb/pnl/__init__.py`（完成 funding/fees/borrow 等 PnL 归因、日维度汇总报表与 CSV/JSON 导出）；已新增 `tests/test_pnl/test_pnl.py`（覆盖归因汇总、日报生成与导出测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `77 passed`）。
