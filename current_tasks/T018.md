待实现功能：
控制 API 服务：`src/arb/control/api.py`（基于 `FastAPI` 暴露 `/health`、`/positions`、`/strategies`、`/commands` 等接口）、`src/arb/control/deps.py`（配置、鉴权、依赖注入）、`src/arb/control/schemas.py`（请求/响应模型）；支持查询当前仓位、策略状态、提交平仓/暂停/恢复命令；编写 API mock 测试与鉴权测试。

已实现功能：
已新增 `src/arb/control/api.py`、`src/arb/control/deps.py`、`src/arb/control/schemas.py` 以及 `src/arb/control/__init__.py`（完成控制 API 服务层、基于 `FastAPI` 可选集成/无依赖 fallback 路由、`/health`、`/positions`、`/strategies`、`/commands` 接口、鉴权与请求响应模型）；已新增 `tests/test_control/test_control_api.py`（覆盖接口鉴权、仓位查询、命令提交与路由注册测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `90 passed`）。
