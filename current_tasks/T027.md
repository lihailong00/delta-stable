待实现功能：
`src/arb/exchange/bitget.py`（REST：签名、账户/余额、现货与永续行情、funding、下单/撤单、符号转换）、`src/arb/ws/bitget.py`（WS：books/ticker/funding 订阅、login、心跳、重连、标准化消息）、更新导出并编写 mock 测试 `tests/test_exchange/test_bitget_exchange.py`、`tests/test_ws/test_ws_bitget.py`。

已实现功能：
已新增 `src/arb/exchange/bitget.py`（实现 Bitget REST：Base64 HMAC-SHA256 签名、账户/余额、现货与永续 ticker/orderbook、funding、spot/mix 下单与撤单、`BTCUSDT ↔ BTC/USDT` 符号转换）；已新增 `src/arb/ws/bitget.py`（`wss://ws.bitget.com/v2/ws/public|private`、`books/ticker` 订阅、`login`、字符串 `ping/pong` 心跳、ticker 内 funding 标准化输出）；已更新导出并新增 mock 测试 `tests/test_exchange/test_bitget_exchange.py`、`tests/test_ws/test_ws_bitget.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `129 passed`）。
