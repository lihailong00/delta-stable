待实现功能：
机会扫描器：`src/arb/scanner/funding_scanner.py`（抓取并排序 funding 机会）、`src/arb/scanner/cost_model.py`（手续费、滑点、借币、转账成本估算）、`src/arb/scanner/filters.py`（流动性、黑白名单、最小收益阈值过滤）；编写 mock 测试。

已实现功能：
已新增 `src/arb/scanner/funding_scanner.py`、`src/arb/scanner/cost_model.py`、`src/arb/scanner/filters.py` 以及 `src/arb/scanner/__init__.py`（完成 funding 机会扫描、净收益与年化收益计算、spread/liquidity 估算、白名单/黑名单与最小阈值过滤）；已新增 `tests/test_scanner/test_funding_scanner.py`（覆盖成本模型、过滤逻辑与机会排序测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `58 passed`）。
