待实现功能：
更新 `src/arb/runtime/exchange_manager.py`、`src/arb/runtime/smoke.py`、`src/arb/runtime/__init__.py` 使其依赖 runtime 协议而不是具体类实现；补充多交易所组合式运行时回归测试 `tests/test_runtime/test_realtime_scanner.py`、`tests/test_runtime/test_smoke.py`；新增架构说明文档 `docs/runtime-composition-guide.md` 解释为什么 runtime 使用组合而不是继承。

已实现功能：
已更新 `src/arb/runtime/exchange_manager.py`、`src/arb/runtime/smoke.py`、`src/arb/runtime/__init__.py`：上层编排层改为依赖 `SnapshotRuntimeProtocol` / `LiveRuntimeProtocol`，不再依赖具体 runtime 类；已更新 `tests/test_runtime/test_realtime_scanner.py`、`tests/test_runtime/test_smoke.py`（新增协议兼容回归测试，验证组合式 runtime 可被 manager/smoke 直接接收）；已新增 `docs/runtime-composition-guide.md` 说明 runtime 选择组合而不是继承的原因与当前分层结构。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `uv run pytest -q`，结果 `159 passed`）。
