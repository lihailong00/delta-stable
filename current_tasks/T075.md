待实现功能：
存储层读写边界模型化；为 `src/arb/storage/repository.py` 引入存储行 schema 和 workflow 持久化 schema，收紧 `save_fill(...)`、`list_orders()`、`list_positions()`、`list_fills()`、`list_workflow_states()` 等接口，不再返回 `list[dict[str, Any]]`；补充测试。

已实现功能：
已新增 `src/arb/storage/schemas.py`（补充 `StoredOrderRow`、`StoredPositionRow`、`StoredFillRow`、`StoredFundingSnapshotRow`、`StoredOrderStatusRow`、`StoredWorkflowStateRow`）；已更新 `src/arb/storage/repository.py`（`save_fill(...)`/workflow 持久化接口收紧为显式模型，`list_*()` 返回 typed rows）；已补充 `tests/test_storage/test_repository.py` 对 typed row 输出和 workflow 存储兼容路径的断言。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_storage/test_repository.py`，结果 `4 passed`）。
