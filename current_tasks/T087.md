待实现功能：
独立网络库的最终验收与发布前 smoke；验证在主仓库和独立包示例中的 HTTP/WS 调用、异常语义、重试/限频配置、类型检查与回归测试，整理发布前 checklist，确认可以上传 GitHub 与 PyPI。

已实现功能：
已完成独立网络库最终验收：`typed_transport` 包的 HTTP/WS 抽离、主仓库兼容层、包级构建与发布资料、离线 examples、最小 CI 均已落地；已执行 `PYTHONPATH=src UV_CACHE_DIR=.uv-cache uv run --no-sync mypy src/typed_transport`、`PYTHONPATH=src UV_CACHE_DIR=.uv-cache uv run --no-sync mypy`、`PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run --no-sync pytest -q`、`UV_CACHE_DIR=.uv-cache uv build packages/typed-transport --out-dir dist/typed-transport`、两个离线 example 脚本，全部通过。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（验收命令已执行完成：`281 passed`；`mypy` 对主仓库与独立包均通过；wheel/sdist 均已成功构建）。
