待实现功能：
监控面与文档更新：更新 `src/arb/monitoring/funding_board.py`、控制面响应、示例和文档，让资金费率监控表明确展示结算周期，并说明不同周期 funding 的比较逻辑和使用方式；补充监控与控制面测试。

已实现功能：
已更新 `src/arb/monitoring/funding_board.py`、`src/arb/control/schemas.py`、`src/arb/integrations/feishu/cards.py`、`src/arb/runtime/pipeline.py`，使 funding board、控制面响应、飞书卡片和机会输出都显示 `funding_interval_hours`；已更新 `examples/single_exchange_scan.py`、`examples/multi_exchange_scan.py` 展示多周期 funding 的归一化比较；已更新 `README.md`、`docs/simple-usage-manual.md`、`docs/operator-guide.md` 说明不同结算周期 funding 的比较逻辑；已补充 monitoring/control/feishu 测试并验证示例输出。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q` 针对 monitoring/control/feishu 的测试，结果 `13 passed`；并运行 `examples/single_exchange_scan.py`、`examples/multi_exchange_scan.py` 验证输出）。
