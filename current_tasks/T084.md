待实现功能：
完善独立网络库的公开 API；在抽离后的 HTTP/WS 模块中补 `aclose()`、client 生命周期管理、`request_json()` / `request_text()` / `request_bytes()` / `request_raw()` 等接口，统一 retry policy、timeout、rate limiter、signer 扩展点，并补充文档和测试。

已实现功能：
已完善独立网络库公开 API：`HttpTransport` 暴露 `request()`、`request_json()`、`request_text()`、`request_bytes()`、`request_raw()`、`aclose()`，统一 timeout/retry/rate limiter/signer；`WebSocketSession` 暴露 `connect()`、`send()`、`receive_once()`、`run_forever()`、`aclose()`；已在 `src/typed_transport/__init__.py` 收口公开符号，并在包级 README 中补充使用方式。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run --no-sync pytest -q tests/test_typed_transport tests/test_net`，生命周期与 transport API 测试通过）。
