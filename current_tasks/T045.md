待实现功能：
实盘资金费率套利验收与文档：新增 `tests/test_integrations/test_funding_arb_e2e.py`（从扫描信号到开平仓闭环的集成测试）、新增 `scripts/run_funding_arb_dry_run.py`（读取配置并运行 dry-run 主服务）、更新 `docs/simple-usage-manual.md`、`docs/live-exchange-onboarding.md` 并新增 `docs/funding-arb-runbook.md`（部署、环境变量、限额、告警、手动接管步骤）。

已实现功能：
已新增 `tests/test_integrations/test_funding_arb_e2e.py`，覆盖从扫描信号到开仓、持仓、平仓闭环的集成测试；已新增 `scripts/run_funding_arb_dry_run.py`，读取参数/环境变量并运行本地 dry-run 主服务；已更新 `docs/simple-usage-manual.md`、`docs/live-exchange-onboarding.md`，并新增 `docs/funding-arb-runbook.md`，补充部署、dry-run、限额、告警和手动接管说明。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_integrations/test_funding_arb_e2e.py`，结果 `1 passed`；并手动运行 `PYTHONPATH=src UV_CACHE_DIR=.uv-cache uv run python scripts/run_funding_arb_dry_run.py --iterations 3 --funding-sequence 0.001 0.0008 -0.0002`，输出 3 轮开平仓状态正常）。
