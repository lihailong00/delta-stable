待实现功能：
`src/arb/exchange/htx.py`（REST：签名、账户/余额、现货与永续行情、funding、下单/撤单、符号转换）、`src/arb/ws/htx.py`（WS：market depth/ticker 订阅、鉴权、ping/pong、重连、标准化消息）、更新导出并编写 mock 测试 `tests/test_exchange/test_htx_exchange.py`、`tests/test_ws/test_ws_htx.py`。

已实现功能：
已新增 `src/arb/exchange/htx.py`（实现 HTX REST：Huobi HMAC-SHA256 查询签名、spot 账户/余额、spot 与 USDT 永续 ticker/orderbook、funding、spot/swap 下单与撤单、`btcusdt/BTC-USDT ↔ BTC/USDT` 符号转换）；已新增 `src/arb/ws/htx.py`（`wss://api.huobi.pro/ws` 与 `/ws/v2`、market `depth/ticker` 订阅、v2 auth、ping/pong 处理、标准化消息解析）；已更新导出并新增 mock 测试 `tests/test_exchange/test_htx_exchange.py`、`tests/test_ws/test_ws_htx.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `141 passed`）。
