待实现功能：
事件化成本模型：更新 `src/arb/backtest/simulator.py`、`src/arb/backtest/report.py`，将回测成本拆分为 `open_fee_rate`、`close_fee_rate`、`rebalance_fee_rate`、`borrow_rate`，只在交易事件发生时扣手续费，并输出 funding / fee / borrow 成本分解；补充测试 `tests/test_backtest/test_backtest.py`，验证开仓、平仓、再平衡与周期借币成本的计算。

已实现功能：
已更新 `src/arb/backtest/simulator.py`、`src/arb/backtest/report.py`，将成本拆分为 `open_fee_rate`、`close_fee_rate`、`rebalance_fee_rate`、`borrow_rate`，并输出 funding / fee / borrow 成本分解；已保留 `fee_rate` 兼容入口；已补充测试 `tests/test_backtest/test_backtest.py`，验证开仓、平仓、再平衡与周期借币成本。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_backtest/test_backtest.py`，结果 `8 passed`）。
