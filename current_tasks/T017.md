待实现功能：
运维配置与任务入口：`src/arb/cli.py`（扫描、执行、回测、报表命令）、`src/arb/settings/strategies.py`（策略参数与币种白名单/黑名单）、`src/arb/settings/exchanges.py`（交易所开关与账户配置）；编写 CLI 测试与配置校验测试。

已实现功能：
已新增 `src/arb/cli.py`、`src/arb/settings/strategies.py`、`src/arb/settings/exchanges.py` 以及 `src/arb/settings/__init__.py`（完成 `scan/execute/backtest/report` 命令入口、策略白名单/黑名单配置校验、交易所开关与账户配置校验）；已新增 `tests/test_cli/test_cli.py`（覆盖 CLI 参数解析、配置合法性与任务入口 smoke 测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `86 passed`）。
