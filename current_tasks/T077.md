待实现功能：
风控与对账边界收紧；为 `src/arb/risk/position_monitor.py`、`src/arb/portfolio/reconciler.py` 改为直接消费显式模型（如 `MarketSnapshot`、`Position`、`Order`、必要时新增 view schema），消除 `dict[str, Any]` / `Mapping[str, Any]` 路径；补充测试。

已实现功能：
已更新 `src/arb/risk/position_monitor.py`（补充 `PositionMonitorSnapshot`，主链优先消费显式 snapshot/view 模型，仅保留兼容映射入口）；已更新 `src/arb/portfolio/reconciler.py`（`ReconciliationIssue`/`ReconciliationReport` 模型化，本地存储行与领域对象统一进入显式边界）；已补充 `tests/test_risk/test_position_monitor.py`、`tests/test_portfolio/test_reconciler.py` 对 typed 输入的断言。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_risk tests/test_portfolio`，结果 `7 passed`）。
