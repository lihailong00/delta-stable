待实现功能：
真实交易所验收与运维工具：`src/arb/runtime/smoke.py`（公共 endpoint 连通、自检、私有鉴权检查）、更新 `src/arb/cli.py`（新增 `live-scan`、`smoke` 命令）、`docs/live-exchange-onboarding.md`（真实接通说明、环境变量、风控建议）；编写命令测试与 smoke mock 测试。

已实现功能：
已新增 `src/arb/runtime/smoke.py`（公共 endpoint 连通、自检、私有鉴权 smoke 检查与结果汇总）；已更新 `src/arb/cli.py`（新增 `live-scan`、`smoke` 命令，并支持命令 handler 分发）；已新增 `docs/live-exchange-onboarding.md`（真实接通说明、环境变量、风控建议与排查顺序）；已新增 `tests/test_runtime/test_smoke.py` 并更新 `tests/test_cli/test_cli.py`（覆盖公共 smoke、私有鉴权 smoke、CLI 新命令与 handler 分发测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `153 passed`）。
