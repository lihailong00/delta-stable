待实现功能：
配置层 Pydantic 化；将 `src/arb/config/__init__.py`、`src/arb/config/live.py`、`src/arb/settings/exchanges.py`、`src/arb/settings/strategies.py` 迁移为显式 schema，统一环境变量解析和配置校验；评估是否引入 `pydantic-settings`，并补充测试。

已实现功能：
已更新 `src/arb/config/__init__.py`、`src/arb/config/live.py`（`AppConfig`、`LiveRuntimeConfig` 改为 Pydantic schema，保留手写 env loader，不额外引入 `pydantic-settings`）；已更新 `src/arb/settings/exchanges.py`、`src/arb/settings/strategies.py`（设置项改为显式 schema，并用 `validate_config()` 做业务校验）；已补充 `tests/test_settings.py` 并更新 `tests/test_config.py`、`tests/test_live_runtime.py`、`tests/test_cli/test_cli.py` 对新配置契约的断言。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_config.py tests/test_settings.py tests/test_live_runtime.py tests/test_bootstrap/test_live_runtime_factory.py tests/test_cli/test_cli.py`，结果 `19 passed`）。
