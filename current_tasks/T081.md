待实现功能：
抽离网络请求库的边界设计与包骨架规划；梳理 `src/arb/net/` 当前职责、外部依赖和 `arb` 业务耦合点，确定独立包的目标范围（HTTP transport、WS session、errors、types、rate limiter），明确包名方案（建议发布名与导入名）、目录结构、兼容策略；补充设计文档。

已实现功能：
已新增独立包源码目录 `src/typed_transport/` 与包级资料目录 `packages/typed-transport/`，明确抽离范围为 `HttpTransport`、`AsyncRateLimiter`、`WebSocketSession`、`HttpRequest`、transport errors；已新增设计文档 `docs/typed-transport-extraction-guide.md`，说明包边界、兼容策略、目录结构与发布前注意事项；已将主仓库 `src/arb/net/` 定位为兼容层，后续业务代码仍可继续沿用原导入路径。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（设计文档与目录骨架已落地，验收见 `T087` 的整体构建与回归测试结果）。
