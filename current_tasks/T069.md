待实现功能：
扫描与年化归一化：更新 `src/arb/scanner/cost_model.py`、`src/arb/scanner/funding_scanner.py`，不再默认按 `8h=3 次/天` 年化；改为根据 `funding_interval_hours` 动态计算 hourly/daily/annualized rate，并确保不同交易所、不同结算周期的 funding 机会可以公平排序；补充 scanner/cost model 测试。

已实现功能：
已更新 `src/arb/scanner/cost_model.py`（新增 `periods_per_day`、`hourly_rate`、`daily_rate`、`normalize_rate`，并将 `annualize_rate` 改为按 `funding_interval_hours` 动态计算）；已更新 `src/arb/scanner/funding_scanner.py`（`FundingOpportunity` 增加 `funding_interval_hours`、`hourly_net_rate`、`daily_net_rate`，扫描时按资金费率结算周期公平年化和排序）；已补充 scanner/cost model 测试，验证 `1h/2h/4h/8h` 场景下的净收益换算和排序逻辑。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run pytest -q` 针对 scanner、runtime scanner 与 funding board 的测试，结果 `13 passed`）。
