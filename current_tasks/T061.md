待实现功能：
共享领域模型 Pydantic 化：重构 `src/arb/models.py` 及相关共享 DTO，将 `Ticker`、`OrderBook`、`FundingRate`、`Order`、`Fill`、`Position`、`WsEvent`、控制命令与通用视图等跨模块对象改为明确的 Pydantic 模型；消除 `to_dict() -> dict[str, Any]` 这类宽泛出口，统一为显式模型导出；更新对应单测与示例。

已实现功能：
已更新 `src/arb/models.py`（将 `Ticker`、`OrderBook`、`FundingRate`、`Order`、`Fill`、`Position` 迁移为 Pydantic 领域模型）；已更新 `src/arb/ws/base.py`（将 `WsEvent` 迁移为 Pydantic 模型并收紧 payload 类型）；已更新 `src/arb/market/normalizer.py` 与相关测试，改用模型导出替代 `dataclass/asdict`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_models.py tests/test_ws/test_base_ws.py tests/test_market/test_market_collector.py`，结果 `10 passed`）。
