待实现功能：
`src/arb/exchange/bybit.py`（REST：签名、账户/余额、现货与永续行情、funding、下单/撤单、符号转换）、`src/arb/ws/bybit.py`（WS：orderbook/ticker 订阅、认证、心跳、重连与标准化消息）、编写 mock 测试 `tests/test_exchange/test_bybit_exchange.py`、`tests/test_ws/test_ws_bybit.py`。

已实现功能：
已新增 `src/arb/exchange/bybit.py`（实现 Bybit v5 REST：`X-BAPI-*` 认证头、`timestamp + api_key + recv_window + query/body` HMAC-SHA256 签名、行情/深度/funding/余额/下单撤单接口、`BTCUSDT ↔ BTC/USDT` 符号转换、WS 认证签名消息生成）；已新增 `src/arb/ws/bybit.py`（`orderbook.50.<symbol>`、`tickers.<symbol>` 订阅、private auth、ping/pong、标准化订单簿与 ticker 消息解析）；已更新导出并新增 mock 测试 `tests/test_exchange/test_bybit_exchange.py`、`tests/test_ws/test_ws_bybit.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `40 passed`）。
