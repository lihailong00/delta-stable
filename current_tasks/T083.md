待实现功能：
抽离 WebSocket 层核心能力；把 `src/arb/net/ws.py` 中通用的连接、重连、订阅恢复、ping/pong 基础能力迁移为独立包模块，去掉对 `arb.ws.schemas`、`ArbModel` 的直接依赖，改为通用消息序列化接口；补充 WS session 测试。

已实现功能：
已新增 `src/typed_transport/ws.py` 与通用消息序列化逻辑 `serialize_message()`，抽离 `WebSocketSession` 的连接、重连、订阅恢复与 `aclose()` 能力；已去掉对 `arb.ws.schemas` 和 `ArbModel` 的直接依赖，统一改为 transport 层的通用消息对象/模型；已新增测试 `tests/test_typed_transport/test_ws_transport.py`，覆盖断线重连、订阅恢复、消息回调与关闭流程。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run --no-sync pytest -q tests/test_typed_transport tests/test_net tests/test_runtime/test_runtime_components.py`，WS 与 runtime 兼容路径通过）。
