待实现功能：
Gate 真实接通：更新 `src/arb/exchange/gate.py`、`src/arb/ws/gate.py` 接入真实 HTTP/WS transport；新增 `src/arb/runtime/gate_runtime.py`（公共行情订阅、私有账户校验、REST/WS 联动）；新增 Gate 公共与私有 smoke 测试/脚本。

已实现功能：
已新增 `src/arb/runtime/gate_runtime.py` 并更新 `src/arb/runtime/__init__.py`（完成 Gate REST/WS 与 `HttpTransport`/`WebSocketSession` 的运行时接线，支持公共 REST 连通校验、私有余额校验、公共快照采集与 `spot.order_book` 订阅）；现有 `src/arb/exchange/gate.py`、`src/arb/ws/gate.py` 已可直接接收真实 transport；已新增 `tests/test_runtime/test_gate_runtime.py`（覆盖公共连通、私有账户校验、公共快照与 `spot.order_book` 订阅测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `122 passed`）。
