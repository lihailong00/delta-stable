待实现功能：
交易级回测结果与报表：更新 `src/arb/backtest/simulator.py`、`src/arb/backtest/report.py`，新增交易记录、持仓时长、交易次数、资金利用率、每笔交易收益等输出结构；补充测试 `tests/test_backtest/test_backtest.py`，验证 trade ledger、报表聚合和回撤计算在阈值策略下保持一致。

已实现功能：
已更新 `src/arb/backtest/simulator.py`、`src/arb/backtest/report.py`、`src/arb/backtest/__init__.py`，新增交易记录、持仓时长、交易次数、资金利用率、平均单笔收益等输出结构；已补充测试 `tests/test_backtest/test_backtest.py`，验证 trade ledger、报表聚合与阈值策略下的回撤和利用率。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_backtest/test_backtest.py`，结果 `9 passed`）。
