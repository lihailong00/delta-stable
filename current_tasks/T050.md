待实现功能：
持仓期风险监控主循环：新增 `src/arb/risk/position_monitor.py`（持续检查 liquidation buffer、basis 异常、naked leg 持续时长、funding 反转、持仓超时）、更新 `src/arb/runtime/funding_arb_service.py`、`src/arb/workflows/close_position.py`（触发自动平仓 / reduce-only）、补充测试 `tests/test_risk/test_position_monitor.py`、`tests/test_runtime/test_funding_arb_service.py`。

已实现功能：
已新增 `src/arb/risk/position_monitor.py`，实现 liquidation buffer、basis 异常、naked leg、funding 反转、持仓超时监控；已更新 `src/arb/runtime/funding_arb_service.py`、`src/arb/risk/__init__.py`，将持仓期异常接入自动平仓主循环；已补充测试 `tests/test_risk/test_position_monitor.py`、`tests/test_runtime/test_funding_arb_service.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_risk/test_position_monitor.py` 与 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_runtime/test_funding_arb_service.py`，结果分别为 `2 passed`、`3 passed`）。
