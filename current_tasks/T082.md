待实现功能：
抽离 HTTP 层核心能力；把 `src/arb/net/http.py`、`src/arb/net/errors.py`、`src/arb/net/schemas.py` 中可复用部分迁移为独立包内部模块，去掉对 `arb.schemas.base`、`market_type` 等业务字段的耦合；保留 `HttpTransport`、`AsyncRateLimiter`、`HttpRequest`、网络错误等公开接口，并补充测试。

已实现功能：
已新增 `src/typed_transport/http.py`、`src/typed_transport/errors.py`、`src/typed_transport/types.py`，抽离 `HttpTransport`、`AsyncRateLimiter`、`HttpRequest`、`NetworkError`、`HttpStatusError` 等 HTTP 核心能力；独立包不再依赖 `arb.schemas.base`，`HttpRequest` 通过 Pydantic 模型承载 headers/params/json/body/timeout，并保留可选兼容字段支撑主仓库旧适配器；已新增测试 `tests/test_typed_transport/test_http_transport.py`，覆盖重试、限频、`request_text()` / `request_bytes()`、状态码异常与 `aclose()`。

完成度：
100%（1/1 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src:. UV_CACHE_DIR=.uv-cache uv run --no-sync pytest -q tests/test_typed_transport tests/test_net`，HTTP 相关测试通过；兼容层回归见 `T085`、`T087`）。
