待实现功能：
真实网络层基础：`src/arb/net/http.py`（基于 `httpx` 的异步 HTTP transport、超时、重试、限频、错误映射、签名钩子）、`src/arb/net/ws.py`（基于 `websockets` 的 WS 会话管理、自动重连、订阅恢复、回调分发）、`src/arb/net/errors.py`（网络层异常）；编写 transport mock 测试与重连测试。

已实现功能：
已新增 `src/arb/net/http.py`、`src/arb/net/ws.py`、`src/arb/net/errors.py` 以及 `src/arb/net/__init__.py`（完成可注入 client/connector 的真实网络基础层、HTTP 超时/重试/限频/错误映射、WS 会话管理、自动重连与订阅恢复，保留 `httpx`/`websockets` 可选依赖入口）；已新增 `tests/test_net/test_http_transport.py`、`tests/test_net/test_ws_transport.py`（覆盖 HTTP 重试、限频、WS 自动重连与订阅恢复测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `python -m unittest discover -s tests -v`，结果 `104 passed`）。
