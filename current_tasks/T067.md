待实现功能：
静态类型门禁与迁移文档：在 `pyproject.toml` 中加入类型检查工具配置（如 `mypy` 或等价方案），为 `src/arb` 建立逐步收紧的类型规则，重点禁止主链新增 `Any` 与未建模的跨模块 `dict`；新增迁移文档说明 Pydantic 建模规范、例外边界和后续新增代码要求；补充 CI/本地检查说明。

已实现功能：
已更新 `pyproject.toml`（新增 `mypy` 门禁配置，先收紧 `schemas/bootstrap/control/market/net/ws` 等已完成模型化的主链边界模块，并启用 `disallow_untyped_defs`、`warn_return_any` 等规则）；已新增 [type-checking-guide.md](/home/longcoding/dev/project/delta_stable/docs/type-checking-guide.md)（说明本地与 CI 检查命令、门禁范围、例外边界和渐进式迁移顺序）；已更新 [pydantic-modeling-guide.md](/home/longcoding/dev/project/delta_stable/docs/pydantic-modeling-guide.md)、[README.md](/home/longcoding/dev/project/delta_stable/README.md) 补充类型门禁入口；已兼容 `src/arb/net/schemas.py` 的 `mypy` 检查。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `PYTHONPATH=src UV_CACHE_DIR=.uv-cache uv run mypy` 与针对性 `pytest`，确认当前门禁范围可运行）。
