待实现功能：
阈值回测示例与使用文档：更新 `examples/backtest_report.py`，新增一个阈值驱动回测示例（如 `examples/backtest_threshold_strategy.py`），并更新 `README.md`、`docs/simple-usage-manual.md`、`docs/operator-guide.md`，说明开仓阈值、平仓阈值、费用参数该如何配置与解读；补充测试 `tests/test_backtest/test_backtest.py` 或相应示例 smoke 测试，确保文档中的示例参数能跑通。

已实现功能：
已更新 `examples/backtest_report.py`（改为基于真实 Binance 历史样本演示阈值驱动回测，展示 `open_threshold`、`close_threshold`、`open_fee_rate`、`close_fee_rate`、`rebalance_fee_rate`、`rebalance_threshold_bps`、`borrow_rate` 配置）；已新增 `examples/backtest_threshold_strategy.py`（用确定性样本展示开仓、持有、再平衡、平仓和交易账本输出）；已更新 `README.md`、`docs/simple-usage-manual.md`、`docs/operator-guide.md`（补充阈值回测参数语义、使用顺序和结果解读），并新增示例参数回归测试 `tests/test_backtest/test_backtest.py`。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_backtest/test_backtest.py`，并手动执行 `PYTHONPATH=src uv run python examples/backtest_report.py`、`PYTHONPATH=src uv run python examples/backtest_threshold_strategy.py` 验证示例输出与文档说明一致）。
