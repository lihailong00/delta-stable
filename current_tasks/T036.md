待实现功能：
订单与账户查询抽象扩展：更新 `src/arb/exchange/base.py`（新增 `fetch_order()`、`fetch_open_orders()`、`fetch_positions()`、`fetch_fills()` 抽象接口与统一返回约定）、`src/arb/models.py`（补充订单查询、成交回报、持仓快照所需字段与模型）；补充契约测试 `tests/test_exchange/test_base_exchange.py`、`tests/test_models.py`，为后续订单状态跟踪和仓位对账打基础。

已实现功能：
已更新 `src/arb/exchange/base.py`（新增 `fetch_order()`、`fetch_open_orders()`、`fetch_positions()`、`fetch_fills()` 统一抽象接口）；已更新 `src/arb/models.py`（扩展 `Order`、新增 `Fill`、补充 `Position` 查询字段与剩余数量属性）；已更新 `tests/test_exchange/test_base_exchange.py`、`tests/test_models.py`（补充新抽象契约测试和模型序列化/剩余数量测试）。

完成度：
100%（3/3 项已完成）。

待测试功能：
无（已执行 `UV_CACHE_DIR=.uv-cache uv run pytest -q tests/test_exchange/test_base_exchange.py tests/test_models.py`，结果 `5 passed`）。
